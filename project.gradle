jar.manifest.attributes (
	'FMLAT': "tconstruct_tabs_at.cfg",
)

apply from: "makalibs.gradle"

repositories {
	maven {
		name = "gt"
		url = "https://gregtech.mechaenetia.com/"
	}
}

dependencies {
    compile "inventorytweaks:InventoryTweaks:1.59-dev:deobf"
}
